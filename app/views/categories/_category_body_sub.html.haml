.post-box
  %table.post-table
    %tr
      %td
        = time_ago_in_words(category.created_at)
        = t(:ago)
  %br/
  %p
    "#{t(:the_category)}: #{@categories.where("id = '#{category.subable_id}'").first.title}"
